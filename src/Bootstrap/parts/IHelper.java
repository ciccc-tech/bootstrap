package Bootstrap.parts;

/** We will use this as a base class from which we can derive Helper Classes
 * 
 * To do that other classes must use the extends or implements keyword to indicate
 * that they want/need to use this interfaces code. 
 */
public interface IHelper 
	{
	}
